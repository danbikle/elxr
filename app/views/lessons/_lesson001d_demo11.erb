<code class='bash'>
# demo12.exs

# This script should demo a function syntax which is different.

# I can omit parenthesis in two next lines:
prt2 = fn input ->
  IO.puts input
end

# When I call prt2, I should remember the period and the parenthesis:
prt2.("Hello Utah")
# Parenthesis required in above line.

</code>
